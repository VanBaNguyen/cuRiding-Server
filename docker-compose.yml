version: '3.8'

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: curiding-server
    ports:
      - "8000:8000"
    env_file:
      - ./server/.env
    restart: unless-stopped
